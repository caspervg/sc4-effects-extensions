# `dynamicParticleEffect`

Status: `Confirmed`

`dynamicParticleEffect` references a named top-level `dynamicParticle`
definition inside an `effect`.

## Syntax

```fx
dynamicParticleEffect ExistingDynamicParticleName [shared child options...]
```
